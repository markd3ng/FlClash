name: build

on:
  push:
    tags:
      - 'v*'

env:
  IS_STABLE: ${{ !contains(github.ref, '-') }}
  FLUTTER_VERSION: '3.44.4'
  GO_VERSION: '1.26'
  MSYS_NO_PATHCONV: 1

jobs:
  version:
    name: Generate Version
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      full_version: ${{ steps.version.outputs.full_version }}
      version_number: ${{ steps.version.outputs.version_number }}
      build_number: ${{ steps.version.outputs.build_number }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Generate Version
        id: version
        env:
          TZ: UTC
        run: |
          set -e
          VERSION_NUMBER=$(grep -E "^version:" pubspec.yaml | sed -E 's/^version:[[:space:]]*([0-9.]+).*/\1/')
          
          UTC_NOW=$(date -u '+%Y-%m-%d %H:%M:%S')
          BUILD_NUMBER=$(date -u -d "$UTC_NOW 8 hours" '+%Y%m%d%H' 2>/dev/null || date -u -v+8H -j -f '%Y-%m-%d %H:%M:%S' "$UTC_NOW" '+%Y%m%d%H')
          
          FULL_VERSION="${VERSION_NUMBER}+${BUILD_NUMBER}"
          
          echo "version_number=$VERSION_NUMBER" >> $GITHUB_OUTPUT
          echo "build_number=$BUILD_NUMBER" >> $GITHUB_OUTPUT
          echo "full_version=$FULL_VERSION" >> $GITHUB_OUTPUT
          
          echo "Generated version: $FULL_VERSION"
          echo "  Version number: $VERSION_NUMBER"
          echo "  Build number: $BUILD_NUMBER"

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        run: flutter test --reporter expanded

  build:
    name: "Build ${{ matrix.name }}"
    needs: [version, test]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: android
            os: ubuntu-latest
            arch: arm64
            name: "Android (arm64)"
          - platform: android
            os: ubuntu-latest
            arch: arm
            name: "Android (arm)"
          - platform: android
            os: ubuntu-latest
            arch: amd64
            name: "Android (x64)"
          - platform: windows
            os: Windows-2022
            arch: amd64
            name: "Windows (x64)"
          - platform: windows
            os: windows-11-arm
            arch: arm64
            name: "Windows (arm64)"
          - platform: macos
            os: macos-15-intel
            arch: amd64
            name: "macOS (Intel x64)"
          - platform: macos
            os: macos-latest
            arch: arm64
            name: "macOS (Apple Silicon)"
          - platform: linux
            os: ubuntu-22.04
            arch: amd64
            name: "Linux (x64)"
          - platform: linux
            os: ubuntu-24.04-arm
            arch: arm64
            name: "Linux (arm64)"

    steps:
      - name: Setup rust
        if: startsWith(matrix.os, 'windows-11-arm')
        run: |
          Invoke-WebRequest -Uri "https://win.rustup.rs/aarch64" -OutFile rustup-init.exe
          .\rustup-init.exe -y --default-toolchain stable
          $cargoPath = "$env:USERPROFILE\.cargo\bin"
          Add-Content $env:GITHUB_PATH $cargoPath

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Android Signing
        if: startsWith(matrix.platform,'android')
        run: |
          echo "${{ secrets.KEYSTORE }}" | base64 --decode > android/app/keystore.jks
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/local.properties
          echo "storePassword=${{ secrets.STORE_PASSWORD }}" >> android/local.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/local.properties

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: |
            core/go.sum

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: master
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: ${{ matrix.platform != 'windows' }}

      - name: Cache Gradle
        if: matrix.platform == 'android'
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/*.gradle*', 'android/gradle/libs.versions.toml', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Cache Cargo
        if: matrix.platform == 'windows'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            services/helper/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('services/helper/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache CocoaPods
        if: matrix.platform == 'macos'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cocoapods
            ~/Library/Caches/CocoaPods
            macos/Pods
          key: ${{ runner.os }}-cocoapods-${{ hashFiles('macos/Podfile', 'macos/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-cocoapods-

      - name: Cache npm
        if: matrix.platform == 'macos'
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('setup.dart') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Get Flutter Dependency
        run: |
          flutter --version
          flutter pub get

      - name: Setup macOS Signing
        if: matrix.platform == 'macos'
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          MACOS_KEYCHAIN_PASSWORD: ${{ secrets.MACOS_KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          CERT_PATH="$RUNNER_TEMP/devid_cert.p12"
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"
          # Decode the base64 Developer ID certificate (.p12)
          printf '%s' "$MACOS_CERTIFICATE" | openssl enc -base64 -d -A > "$CERT_PATH"
          # Create a dedicated temporary keychain and make it searchable
          security create-keychain -p "$MACOS_KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$MACOS_KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -P "$MACOS_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          # Import the Apple Developer ID intermediate CA so the chain is complete (best-effort)
          curl -fsSL https://www.apple.com/certificateauthority/DeveloperIDG2CA.cer -o "$RUNNER_TEMP/DeveloperIDG2CA.cer" && security import "$RUNNER_TEMP/DeveloperIDG2CA.cer" -k "$KEYCHAIN_PATH" || true
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$MACOS_KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH" >/dev/null
          # Prepend the temp keychain to the search list (keep existing ones)
          security list-keychains -d user -s "$KEYCHAIN_PATH" $(security list-keychains -d user | sed s/\"//g)
          rm -f "$CERT_PATH"
          echo "Available Developer ID signing identities:"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

      - name: Build Application
        id: build
        shell: bash
        env:
          FLUTTER_VERSION_NUMBER: ${{ needs.version.outputs.version_number }}
          FLUTTER_BUILD_NUMBER: ${{ needs.version.outputs.build_number }}
          PROFILE_KEY: ${{ secrets.PROFILE_KEY }}
          BASE_DOMAIN: ${{ secrets.BASE_DOMAIN }}
          SPARE_DOMAIN: ${{ secrets.SPARE_DOMAIN }}
          API_DOMAIN: ${{ secrets.API_DOMAIN }}
          SPARE_API_DOMAIN: ${{ secrets.SPARE_API_DOMAIN }}
          FLCLASH_APP_SECRET: ${{ secrets.FLCLASH_APP_SECRET }}
          DNS_AUTH_PRIVATE_KEY: ${{ secrets.DNS_AUTH_PRIVATE_KEY }}
          DNS_AUTH_DOMAINS: ${{ secrets.DNS_AUTH_DOMAINS }}
        run: |
          set -e
          BUILD_ARGS="${{ matrix.platform }}"
          [ -n "${{ matrix.arch }}" ] && BUILD_ARGS="$BUILD_ARGS --arch ${{ matrix.arch }}"
          BUILD_ARGS="$BUILD_ARGS --out app"
          [ "${{ env.IS_STABLE }}" == "true" ] && BUILD_ARGS="$BUILD_ARGS --env stable"
          dart setup.dart $BUILD_ARGS

          if [ ! -d ./dist ] || [ -z "$(find ./dist -type f -print -quit)" ]; then
            echo "Build produced no artifacts in ./dist" >&2
            exit 1
          fi
          
          find ./dist -name "*.exe" -type f -exec sh -c 'mv "$1" "${1%.exe}-setup.exe"' _ {} \;
          find ./dist -name "*.appimage" -type f -exec sh -c 'mv "$1" "${1%.appimage}.AppImage"' _ {} \;

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: artifact-${{ matrix.platform }}${{ matrix.arch && format('-{0}', matrix.arch) }}
          path: ./dist
          overwrite: true
          if-no-files-found: error

  changelog:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs: [ version, build ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        if: ${{ env.IS_STABLE == 'true' }}
        with:
          fetch-depth: 0
          ref: refs/heads/main
      - name: Generate
        if: ${{ env.IS_STABLE == 'true' }}
        run: |
          last_ver=$(grep -m1 '^## ' CHANGELOG.md 2>/dev/null | sed 's/^## //')
          
          tags=($(git tag --merged HEAD --sort=-creatordate))
          
          temp="NEW_CHANGELOG.md"
          > "$temp"
          
          for i in "${!tags[@]}"; do
              curr="${tags[i]}"
              [[ "$curr" == "$last_ver" ]] && break
          
              prev="${tags[i+1]}"
              range="${prev:+$prev..}$curr"
          
              echo -e "## $curr\n" >> "$temp"
              git log --no-merges --pretty=format:"%B" "$range" | \
              awk '!/Update changelog/ && NF {print "- " $0 "\n"}' >> "$temp"
          done
          [ -f CHANGELOG.md ] && cat CHANGELOG.md >> "$temp"
          
          mv "$temp" CHANGELOG.md

      - name: Commit
        if: ${{ env.IS_STABLE == 'true' }}
        run: |
          git add CHANGELOG.md
          if ! git diff --cached --quiet; then
            echo "Commit pushing"
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git config --local user.name "github-actions[bot]"
            git commit -m "Update changelog"
            git push
            if [ $? -eq 0 ]; then
              echo "Push succeeded"
            else
              echo "Push failed"
              exit 1
            fi
          fi

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  upload:
    permissions: write-all
    needs: [ version, build ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download
        uses: actions/download-artifact@v4
        with:
          path: ./dist/
          pattern: artifact-*
          merge-multiple: true

      - name: Generate release.md
        run: |
          tags=($(git tag --merged HEAD --sort=-creatordate))
          preTag=$(curl -s "https://api.github.com/repos/pickrui/FlClash/releases/latest" | grep -Po '"tag_name": "\K.*?(?=")' || echo "")
          out="release.md"
          > "$out"
          for i in "${!tags[@]}"; do
            curr="${tags[i]}"
            [[ "$curr" == "$preTag" ]] && break

            prev="${tags[i+1]}"
            range="${prev:+$prev..}$curr"

            echo -e "## $curr\n" >> "$out"
            git log --no-merges --pretty=format:"%B" "$range" | \
            awk '!/Update changelog/ && NF {print "- " $0 "\n"}' >> "$out"
          done

      - name: Patch release.md
        run: |
          version=$(echo "${{ github.ref_name }}" | sed 's/^v//')
          sed "s|VERSION|$version|g" ./.github/release_template.md >> release.md

      - name: Release
        if: ${{ env.IS_STABLE == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          files: ./dist/*
          body_path: './release.md'

      - name: Update Version via oixCloud API
        if: success() && env.IS_STABLE == 'true'
        continue-on-error: true
        run: |
          API_KEY="${{ secrets.FLCLASH_KEY }}"
          API_URL="https://${{ secrets.API_DOMAIN }}/api/v1/version/update"
          FULL_VERSION="${{ needs.version.outputs.full_version }}"
          
          if [ -z "$API_KEY" ]; then
            echo "⚠️  Skipping version update: API_KEY secret not configured"
            exit 0
          fi
          
          if [ -z "$FULL_VERSION" ]; then
            echo "❌ Error: Version number not available"
            exit 1
          fi
          
          echo "📦 Updating oixCloud version to: $FULL_VERSION"
          
          response=$(curl -s -w "\n%{http_code}" -X POST "$API_URL" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            --data-urlencode "key=$API_KEY" \
            --data-urlencode "version=$FULL_VERSION")

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          ret_code=$(echo "$body" | grep -oE '"ret"[[:space:]]*:[[:space:]]*[0-9]+' | grep -oE '[0-9]+$' || echo "")

          echo "Response body: $body"
          echo "HTTP status: $http_code   Business ret: ${ret_code:-<none>}"

          if [ "$http_code" = "200" ] && [ "$ret_code" = "200" ]; then
            echo "✅ Version updated successfully on oixCloud"
            echo "   Version: $FULL_VERSION"
            echo "   API: $API_URL"
          else
            echo "❌ Failed to update version"
            echo "   HTTP Status: $http_code"
            echo "   Business ret: ${ret_code:-<none>}"
            echo "   Response: $body"
            exit 1
          fi
