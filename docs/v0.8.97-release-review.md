# v0.8.97 发布审查

范围：复核上游 v0.8.97 的核心、界面、数据库与平台迁移，并处理 Android 9/13 弱网延迟测试及 Windows 开机静默启动反馈

## 修复与整理

- 手动测速共享并发上限从 50 降至 8，Android 批量首轮使用 4 路；出现连续失败或慢响应后降至 2 路，减少探测本身对弱链路的争抢
- 首轮保持 5 秒网络预算，只对失败节点复测一次，复测预算 15 秒、并发 2 路；单节点测试使用同样的失败复测预算，成功结果立即显示
- RPC 预算随网络预算增加 2 秒余量，换配置或新一轮测速后继续丢弃过期结果和排队工作；真正不可达的节点仍显示失败
- SSID 暂停／恢复任一步骤失败时，将已应用状态置为未知，下次同时协调 Core 和 VPN；清空排除列表后保留轮询，直到恢复成功
- setup.dart 删除已失效的工具函数及重复字段，命令参数改为结构化列表，支持带空格路径，任何子命令非零退出都会终止构建并保留脱敏错误
- 修复发布 CI 对 Dart CLI 使用不支持的 `--no-fatal-infos` 参数；`dart analyze` 默认不将 info 视为失败，仍检查 warning 和 error
- 修复 Linux NDK 不附带 libclang 时过早拒绝 Android Rust 构建的问题：优先显式主机库路径，再查 NDK，最后允许 clang-sys 发现已安装的系统库；补充 5 项回归
- Windows 配置重置回归使用原生目录分隔符构造嵌套文件名，并通过持锁句柄检查锁文件内容，兼容 Windows 强制字节锁；未改动备份格式或重置行为
- 自动更新仅在应用内提示，用户手动检查更新时才显示窗口；启动日志记录自启动标记、静默设置及最终显示决策，不记录原始参数；清理原生启动中无作用的首帧回调和过期说明
- 修复 Sudoku poll 下行响应在持续传输时仍被 30 秒总期限截断的问题，改为随连接关闭取消请求，保留服务端结束响应后的续接及半关闭；虚拟时间夹具在旧实现稳定失败，覆盖修复后 50 秒持续响应、主动关闭和 EOF
- 修复 Sudoku 自动切换隧道和 HTTP 授权重试复用早期握手的问题：响应丢失后为新请求生成独立 nonce 和密钥状态，保留服务端防重放检查；真实协议夹具在旧实现稳定触发 404，修复后使用成功请求的密钥解密下行数据
- 将耗时的 Sudoku 入站全变体独立为必过 CI 作业，其他核心测试完整保留，避免所有协议共用一个 15 分钟包级限额
- Windows 代理原生测试宿主使用最小 Flutter 入口，避免新 SDK 的计数器模板语法超过宿主声明的 Dart 语言版本；从 PowerShell 脚本提取真实生成内容后，隔离构建 bundle 已通过
- Windows 原生 hook 缺少 `LOCALAPPDATA` / `USERPROFILE` 时，给 Go 提供仓库内显式缓存与模块目录，保留原有可用配置；真实离线 Go 编译覆盖过滤后的环境，并接入 Windows 必过检查
- macOS 打包显式通过 `FLUTTER_XCODE_ARCHS` 传递所选架构，避免 Flutter Release 默认双架构与单架构 Core 校验冲突；新增使用占位参数的桌面 Release 检查，保留原生 hook 详细诊断日志
- 构建号下限设为 `2026092917`，高于用户反馈中的 `2026092916`；CI 使用时钟编号与 pubspec 下限的较大值，避免 Android 覆盖安装被误判为降级

旧 `plugins/tray_manager`、`third_party/quickjs-c-bridge` 及其 Linux/Windows 构建胶水仍保留在仓库中，当前依赖和平台构建使用 `plugins/tray` 与 `rust_api`，没有引用旧实现；删除操作尚待具体路径授权，不影响当前构建

## 验证

| 检查 | 结果 |
| --- | --- |
| 根包 Dart 分析、lib/test/setup.dart/tool 与两个 hook 入口分析 | 无问题 |
| 代码生成 | build_runner 成功；同步动作 provider 的生成哈希，模型仅有空白输出并已规范化 |
| Flutter 根包及 proxy、wifi_ssid、tray 插件 | 本次 Linux CI 1453 项通过、6 项环境跳过；本地验证加载真实打包 Rust framework 与隔离 Go Core |
| Go Core | 本地及 Linux CI 无 CGO 桌面模式 vet 与完整竞态测试通过；真实慢 HTTP 服务器验证短预算失败、长预算恢复 |
| Sudoku poll 回归 | httpmask 整个包的竞态测试重复 10 轮通过，vet 通过；所有 Sudoku 入站变体在限制 2 路 CPU 的本地完整竞态复验中通过；独立 Linux CI 作业全部变体通过（174 秒） |
| Sudoku 授权重试回归 | 丢失授权响应后重试及 stream 回退 poll 场景连续 5 轮通过，验证独立 nonce、成功请求密钥和服务端防重放；全部传输包 vet 与竞态测试通过；修复后全部 Sudoku 入站变体在限制 2 路 CPU 的竞态复验中通过（121 秒） |
| setup_hooks | 分析无问题，54 项通过，新增过滤环境下的真实 Go 编译及已有目录配置保留回归 |
| Rust API / 共享 Helper | 39 / 22 项通过，格式检查通过 |
| Android | 三 ABI 完整 Debug APK 通过，包内核验 Go / JNI / Rust 库及版本号；全部 102 项 JVM 单元测试通过，含新增 3 项 SSID 部分失败恢复测试 |
| macOS | 删除预编译 Core 后完整 Debug 与 Release 构建通过；Release 主程序、Core、Rust framework 均为所选 arm64，包内版本号与发布元数据一致；ARM64 与 Intel 云端 Release 占位参数构建均通过 |
| 发布版本生成 | 执行真实 CI 脚本，验证时钟编号、最低构建号及非法值拒绝 |
| Windows 完整门禁 | Windows CI Helper 24 项、Flutter 80 项、Go 构建与缓存 6 项、原生代理退出 7 项通过，另 1 项因环境跳过；GoogleTest 下载首次遇到 GitHub HTTP 504，随后原地址下载及测试成功 |
| Windows 静默启动专项 | 本地及 CI 验证静默参数、显示／隐藏、后台／手动更新及移动端提示；原生 GUI 夹具使用真实插件的显示、隐藏、聚焦和恢复方法，四种启动器指令与手动打开均已在 Windows CI 通过；本地专项 28 项通过、1 项平台跳过 |

Windows 控制台与 GUI 原生对照均未复现首次 `ShowWindow(SW_HIDE)` 反向显示窗口，因此撤回该根因假设和对应原生补丁；已确认并修复的是自动检查更新主动显示窗口的入口，具体用户登录环境仍需新版本复测

本次应用构建仅使用公开占位参数，原有 Core 文件和本机构建设置在验证后恢复；占位参数产物不用于正式分发
之前已通过的 Linux Helper 离线安装夹具、数据库迁移、TV 焦点与平台专项覆盖见 [UI 与平台迁移记录](upstream-v0.8.97-ui-platform-migration.md)

## 验收边界与发布

未执行 Android 9/13 实机小部件视觉、真实移动弱网、SSID 权限和漫游验收；未执行 Windows/Linux 真实客户端桌面验收及真实 systemd/polkit/包管理器安装事务
慢 HTTP 与并发受限夹具证明延长预算、减少竞争可以恢复部分误超时，不能保证无线链路断开、DNS 或节点不可达时仍能成功；大量失败节点的整批测试耗时会增加

本轮十项测试及版本门禁全部通过，Android 三架构和 Linux 两架构正式构建通过；macOS 架构设置与 Windows Go hook 缓存问题修复后，四个桌面架构的隔离 Release 编译均通过，更新后的 Windows 门禁全部通过；正式签名与安装包打包继续由标签工作流验证

版本标记为 `0.8.97+2026092917`；正式产物由 `v0.8.97` 标签触发原有发布工作流，经过 Linux/Windows/Android 原生检查及各平台构建后发布
